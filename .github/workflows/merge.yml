name: 规则合并

on:
  workflow_dispatch:

jobs:
  merge:
    runs-on: ubuntu-latest
    steps:
      # 将下面这一行从 @v4 改为 @v5
      - name: 检出代码
        uses: actions/checkout@v5

      - name: 测试执行
        run: |
          echo "工作流开始执行"
